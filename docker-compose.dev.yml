# docker-compose.yml — All AI services + infrastructure
# Usage: docker-compose up           (starts everything)
#        docker-compose up -d        (starts in background)
#        docker-compose logs -f      (follow logs)
#
# For local dev (Python running on host, not in Docker):
#   docker-compose up rabbitmq redis
#   Then run `python main.py` directly on your machine.

services:

  # ── RabbitMQ ────────────────────────────────────────────────────────
  rabbitmq:
    image: rabbitmq:3-management
    container_name: rabbitmq
    ports:
      - "5672:5672"      # AMQP protocol
      - "15672:15672"    # Management UI → http://localhost:15672
    environment:
      RABBITMQ_DEFAULT_USER: guest
      RABBITMQ_DEFAULT_PASSWORD: guest
    healthcheck:
      test: rabbitmq-diagnostics -q ping
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped
    networks:
      - eduvi

  # ── Redis ───────────────────────────────────────────────────────────
  redis:
    image: redis:7-alpine
    container_name: redis
    ports:
      - "6379:6379"
    healthcheck:
      test: redis-cli ping
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped
    networks:
      - eduvi

  # ── Lesson Analysis Worker ──────────────────────────────────────────
  lesson-analysis:
    build:
      context: ./lesson_analysis
      dockerfile: Dockerfile
    container_name: lesson-analysis
    depends_on:
      rabbitmq:
        condition: service_healthy
    environment:
      # RabbitMQ
      RABBITMQ_HOST: rabbitmq
      RABBITMQ_PORT: 5672
      RABBITMQ_USER: guest
      RABBITMQ_PASSWORD: guest
      # Neo4j (Aura — cloud hosted)
      NEO4J_URI: ${NEO4J_URI}
      NEO4J_USER: ${NEO4J_USER}
      NEO4J_PASSWORD: ${NEO4J_PASSWORD}
      # Google Cloud
      GCS_BUCKET_NAME: ${GCS_BUCKET_NAME}
      GOOGLE_CLOUD_PROJECT: ${GOOGLE_CLOUD_PROJECT}
      GOOGLE_APPLICATION_CREDENTIALS: /app/credentials/gcp-key.json
      # Helicone Observability (optional — leave empty to call Vertex AI directly)
      HELICONE_API_KEY: ${HELICONE_API_KEY:-}
    volumes:
      - ./private/gcp-key.json:/app/credentials/gcp-key.json:ro
    dns:
      - 8.8.8.8
      - 8.8.4.4
    restart: unless-stopped
    networks:
      - eduvi

  # ── Slide Generator Worker ───────────────────────────────────────────
  slide-generator:
    build:
      context: ./slide_generator
      dockerfile: Dockerfile
    container_name: slide-generator
    depends_on:
      rabbitmq:
        condition: service_healthy
    environment:
      # RabbitMQ
      RABBITMQ_HOST: rabbitmq
      RABBITMQ_PORT: 5672
      RABBITMQ_USER: guest
      RABBITMQ_PASSWORD: guest
      # Google Cloud
      GOOGLE_CLOUD_PROJECT: ${GOOGLE_CLOUD_PROJECT}
      GOOGLE_APPLICATION_CREDENTIALS: /app/credentials/gcp-key.json
      # Helicone Observability (optional — leave empty to call Vertex AI directly)
      HELICONE_API_KEY: ${HELICONE_API_KEY:-}
      # Neo4j (Aura — cloud hosted)
      NEO4J_URI: ${NEO4J_URI}
      NEO4J_USER: ${NEO4J_USER}
      NEO4J_PASSWORD: ${NEO4J_PASSWORD}
    volumes:
      - ./private/gcp-key.json:/app/credentials/gcp-key.json:ro
    dns:
      - 8.8.8.8
      - 8.8.4.4
    restart: unless-stopped
    networks:
      - eduvi

  # ── Video Generator Worker ───────────────────────────────────────────
  # Scale for concurrent users: docker compose up --scale video-generator=3
  video-generator:
    build:
      context: .
      dockerfile: video_generator/Dockerfile
    depends_on:
      rabbitmq:
        condition: service_healthy
    environment:
      # RabbitMQ
      RABBITMQ_HOST: rabbitmq
      RABBITMQ_PORT: 5672
      RABBITMQ_USER: guest
      RABBITMQ_PASS: guest
      # Low-CPU profile: keep per-container workload small, then scale replicas.
      PREFETCH_COUNT: ${VIDEO_PREFETCH_COUNT:-1}
      MAX_SLIDE_CONCURRENCY: ${VIDEO_MAX_SLIDE_CONCURRENCY:-1}
      RENDER_CONCURRENCY: ${VIDEO_RENDER_CONCURRENCY:-1}
      TTS_CONCURRENCY: ${VIDEO_TTS_CONCURRENCY:-1}
      FFMPEG_CONCURRENCY: ${VIDEO_FFMPEG_CONCURRENCY:-1}
      PROBE_CONCURRENCY: ${VIDEO_PROBE_CONCURRENCY:-1}
      FFMPEG_THREADS: ${VIDEO_FFMPEG_THREADS:-1}
      FFMPEG_PRESET: ${VIDEO_FFMPEG_PRESET:-ultrafast}
      # 15fps is enough for still-image slides; reduces encode time ~37% vs 24fps.
      VIDEO_FPS: ${VIDEO_FPS:-15}
      VIDEO_WIDTH: ${VIDEO_WIDTH:-960}
      VIDEO_HEIGHT: ${VIDEO_HEIGHT:-540}
      PROGRESS_STEP_GRANULARITY: ${VIDEO_PROGRESS_STEP_GRANULARITY:-15}
      PERSIST_PROCESSING_EVENTS: ${VIDEO_PERSIST_PROCESSING_EVENTS:-false}
      # Keep Chromium browser alive across requests — avoids cold-start overhead.
      CLEANUP_BROWSER_EACH_REQUEST: ${VIDEO_CLEANUP_BROWSER:-false}
      # 64k is sufficient for TTS speech audio; saves ~33% vs 96k.
      AUDIO_BITRATE: ${VIDEO_AUDIO_BITRATE:-64k}
      # Google Cloud
      GCS_BUCKET_NAME: ${GCS_BUCKET_NAME}
      VIDEO_GCS_PREFIX: ${VIDEO_GCS_PREFIX:-generated_videos}
      VIDEO_RETURN_GCS_URI: ${VIDEO_RETURN_GCS_URI:-true}
      GOOGLE_CLOUD_PROJECT: ${GOOGLE_CLOUD_PROJECT}
      GOOGLE_APPLICATION_CREDENTIALS: /app/credentials/gcp-key.json
    cpus: ${VIDEO_GENERATOR_CPUS:-1.50}
    # Cap memory so OOM is caught early rather than silent container kill.
    mem_limit: ${VIDEO_GENERATOR_MEM_LIMIT:-768m}
    memswap_limit: ${VIDEO_GENERATOR_MEM_LIMIT:-768m}
    volumes:
      - ./private/gcp-key.json:/app/credentials/gcp-key.json:ro
      - ./videos:/app/videos
    dns:
      - 8.8.8.8
      - 8.8.4.4
    restart: unless-stopped
    networks:
      - eduvi

  # ── Game Quiz Worker ────────────────────────────────────────────────
  # Generates gesture-interactive quiz JSON from edited slide content + template.
  game-quiz:
    build:
      context: .
      dockerfile: game_quiz/Dockerfile
    depends_on:
      rabbitmq:
        condition: service_healthy
    environment:
      # RabbitMQ
      RABBITMQ_HOST: rabbitmq
      RABBITMQ_PORT: 5672
      RABBITMQ_USER: guest
      RABBITMQ_PASS: guest
      GAME_QUIZ_REQUEST_QUEUE: ${GAME_QUIZ_REQUEST_QUEUE:-game.quiz.requests}
      GAME_QUIZ_RESULT_QUEUE: ${GAME_QUIZ_RESULT_QUEUE:-game.quiz.results}
      GAME_QUIZ_PREFETCH_COUNT: ${GAME_QUIZ_PREFETCH_COUNT:-2}
      GAME_QUIZ_LOG_RESULT_PAYLOAD: ${GAME_QUIZ_LOG_RESULT_PAYLOAD:-true}
      GAME_QUIZ_LOG_RESULT_MAX_CHARS: ${GAME_QUIZ_LOG_RESULT_MAX_CHARS:-4000}
      # Google Cloud / Vertex AI
      GOOGLE_CLOUD_PROJECT: ${GOOGLE_CLOUD_PROJECT}
      VERTEX_AI_LOCATION: ${VERTEX_AI_LOCATION:-us-central1}
      GOOGLE_APPLICATION_CREDENTIALS: /app/credentials/gcp-key.json
      HELICONE_API_KEY: ${HELICONE_API_KEY:-}
    volumes:
      - ./private/gcp-key.json:/app/credentials/gcp-key.json:ro
    dns:
      - 8.8.8.8
      - 8.8.4.4
    restart: unless-stopped
    networks:
      - eduvi

  # ── Textbook Ingestion Worker ───────────────────────────────────────
  # textbook-ingestion:
  #   build:
  #     context: ./textbook_ingestion
  #     dockerfile: Dockerfile
  #   container_name: textbook-ingestion
  #   depends_on:
  #     rabbitmq:
  #       condition: service_healthy
  #   environment:
  #     # RabbitMQ
  #     RABBITMQ_HOST: rabbitmq
  #     RABBITMQ_PORT: 5672
  #     RABBITMQ_USER: guest
  #     RABBITMQ_PASSWORD: guest
  #     # Neo4j (Aura — cloud hosted)
  #     NEO4J_URI: ${NEO4J_URI}
  #     NEO4J_USER: ${NEO4J_USER:-neo4j}
  #     NEO4J_PASSWORD: ${NEO4J_PASSWORD}
  #     # Google Cloud
  #     GCS_BUCKET_NAME: ${GCS_BUCKET_NAME}
  #     GOOGLE_CLOUD_PROJECT: ${GOOGLE_CLOUD_PROJECT}
  #     GOOGLE_APPLICATION_CREDENTIALS: /app/credentials/gcp-key.json
  #   volumes:
  #     - ./private/gcp-key.json:/app/credentials/gcp-key.json:ro
  #   restart: unless-stopped
  #   networks:
  #     - eduvi

  # ── Curriculum Ingestion Worker ─────────────────────────────────────
  curriculum-ingestion:
    build:
      context: ./curriculum_ingestion
      dockerfile: Dockerfile
    container_name: curriculum-ingestion
    depends_on:
      rabbitmq:
        condition: service_healthy
    environment:
      # RabbitMQ
      RABBITMQ_HOST: rabbitmq
      RABBITMQ_PORT: 5672
      RABBITMQ_USER: guest
      RABBITMQ_PASSWORD: guest
      # Neo4j (Aura — cloud hosted, same instance as lesson-analysis)
      NEO4J_URI: ${NEO4J_URI}
      NEO4J_USER: ${NEO4J_USER}
      NEO4J_PASSWORD: ${NEO4J_PASSWORD}
      # Google Cloud
      GCS_BUCKET_NAME: ${GCS_BUCKET_NAME}
      GOOGLE_CLOUD_PROJECT: ${GOOGLE_CLOUD_PROJECT}
      GOOGLE_APPLICATION_CREDENTIALS: /app/credentials/gcp-key.json
      # Helicone Observability (optional)
      HELICONE_API_KEY: ${HELICONE_API_KEY:-}
    volumes:
      - ./private/gcp-key.json:/app/credentials/gcp-key.json:ro
    dns:
      - 8.8.8.8
      - 8.8.4.4
    restart: unless-stopped
    networks:
      - eduvi

  # ── Dozzle (Real-time Log Viewer) ───────────────────────────────────
  dozzle:
    image: amir20/dozzle:latest
    container_name: dozzle
    ports:
      - "9999:8080"     # Web UI → http://localhost:9999
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
    restart: unless-stopped
    networks:
      - eduvi

networks:
  eduvi:
    driver: bridge
